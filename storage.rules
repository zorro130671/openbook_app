// firestore.rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMP DEV: allow public read so web works even when not signed in
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
